#make a list of the numbers from one to one million, and then use min() and max() to make sure your list is actually starts at one and ends at one million. Also use the sum() function to see how quickly python can add a million numbers 
millions = list(range(1, 1000001))
print(min(millions))
print(max(millions))
print(sum(millions))

